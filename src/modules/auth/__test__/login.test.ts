import { app } from 'core/app';
import request from 'supertest';

describe('Login', () => {
  it('should throw error for no payload', () => {
    // request(app).post('/api/auth/login').send({}).expect(400);

    console.log('test');
  });
});
